import type { FC } from 'react';
import type { Recipe } from '@recipe-box/shared';

interface ModifiedResultProps {
  recipe: Recipe;
}

export const ModifiedResult: FC<ModifiedResultProps> = (_props) => {
  throw new Error('Not implemented');
};
