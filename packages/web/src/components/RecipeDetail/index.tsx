import type { FC } from 'react';
import type { Recipe } from '@recipe-box/shared';

interface RecipeDetailProps {
  recipe: Recipe;
}

export const RecipeDetail: FC<RecipeDetailProps> = (_props) => {
  throw new Error('Not implemented');
};
